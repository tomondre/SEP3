syntax="proto3";
option java_multiple_files = true;
package networking.order;

message ProtobufMessage{
  string messageOrObject = 1;
}

service OrderService {
  rpc createOrder (ProtobufMessage) returns (ProtobufMessage);
}